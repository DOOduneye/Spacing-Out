package model.Query;

public enum QueryType {
  AND,
  DELETE,
  EXCEPT,
  FROM,
  GROUP,
  HAVING,
  INSERT,
  INTERSECT,
  JOIN,
  LIMIT,
  ON,
  ORDER,
  UNION,
  UPDATE,
  WHERE,
  VALUES,
  SELECT,
}
